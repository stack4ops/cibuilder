#!/bin/sh

# if ! docker run --rm --name $container $test_image docker buildx version | grep "github.com/docker/buildx"; then
#   log 0 "[failed] Test failed!"
#   exit 1
# fi

log 1 "[warn] Test not implemented"
